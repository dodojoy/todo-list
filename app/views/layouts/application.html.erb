<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "DODO" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="<%= asset_path('owl-icon.svg') %>" type="image/svg+xml">
    <link rel="apple-touch-icon" href="<%= asset_path('owl-icon.svg') %>">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= favicon_link_tag asset_path('owl-icon.svg'), type: "image/svg+xml" %>
  </head>

  <body class="bg-custom-black text-custom-white">
  <%= render "layouts/navbar" %>
    <main class="container mx-auto px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
